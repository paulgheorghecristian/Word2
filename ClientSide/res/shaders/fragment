#version 330 core

uniform vec4 colorVector;

in vec2 textureCoords;
out vec4 outColor;

uniform sampler2D textureSampler;
uniform int hasTexture;

void main(){
    if(hasTexture == 1){
        outColor = texture(textureSampler, textureCoords);
    }else{
        outColor = colorVector;
	}
}
